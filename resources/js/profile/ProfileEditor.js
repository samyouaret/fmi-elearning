import React from 'react';
import ReactDOM from 'react-dom';

export default class ProfileEditor extends React.Component {
  render(){
    return (
      <button className="btn btn-lg btn-primary">editor</button>
    );
  }
}
